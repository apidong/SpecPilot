Let me read all the key source files in parallel to begin the review.
Now let me read the remaining critical source files:
Now let me read the worker source, common utilities, shared package, and remaining files:
Now let me read the remaining files: DTOs, migrations, frontend stores, and the existing review:
Let me read the frontend stores and API client:
Now I have all the information needed. Let me write the report.
Now I have thoroughly reviewed the entire codebase. Let me compile the findings into the report.
Report saved to `review/20260516-142732-bug-review.md`. Summary of the 15 findings:

**CRITICAL (2):**
- **C-1** `auth.controller.ts:49` ΓÇö Logout reads `req.jwtPayload` which is never set (Passport sets `req.user`), so the JWT denylist is never populated. Token revocation is completely broken.
- **C-2** `executions.gateway.ts:15` ΓÇö WebSocket CORS is `origin: '*'` regardless of the API's `CORS_ORIGIN` setting, allowing cross-site WebSocket hijacking in production.

**HIGH (5):**
- **H-1** `auth.service.ts:24-35` ΓÇö Register has a check-then-insert race with no unique index on `users.email`.
- **H-2** `agents.service.ts:20-29` ΓÇö Default agent uniqueness check has TOCTOU race; DB constraint will surface unhandled `QueryFailedError`.
- **H-3** `consumer.ts:50-151` ΓÇö `removeWorktree()` is defined but never called. Every execution leaks a git worktree on disk.
- **H-4** `tickets.service.ts:85-89` ΓÇö No guard against modifying a `Running` ticket; also `status` field bypass risk.
- **H-5** `specs.controller.ts:122-131` ΓÇö `restore()` bypasses authorization by casting `{ id: specId } as any`.

**MEDIUM (6):** ArtifactType param unvalidated, Redis denylist TTL resets on shared key, naive `command.split(' ')`, executions controller missing authz, version endpoints missing authz, empty `WORKER_SECRET` default.

**LOW (2):** `--no-verify` on git commit, frontend 401 desync.

