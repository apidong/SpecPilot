Let me read all the critical source files in parallel batches.
Now let me read the remaining files:
Now I have all the information needed. Let me write the report.
Report saved to `review/20260516-154646-bug-review.md`. Reviewed ~50 files across API, Worker, Web, and Shared packages.

**14 findings total:**

| Severity | Count | Key Issues |
|----------|-------|------------|
| CRITICAL | 3 | JWT denylist single-key TTL corruption; WebSocket bypasses denylist check; worktree path mismatch (commit always fails) |
| HIGH | 5 | Dead compensation logic in concurrent guard; TOCTOU race in register; no WS ownership check; non-atomic ticket run; stop signal race |
| MEDIUM | 5 | Windows command validation mismatch; unbounded batch logs; diff counting off-by-headers; Redis not closed on shutdown; command arg splitting |
| LOW | 4 | Raw table join; `as any` cast; CORS default `*`; execution status not filtered |

The 3 most impactful bugs to fix first: **C1** (JWT denylist corruption), **C3** (worktree path mismatch ΓÇö commits are completely broken), and **H1** (dead compensation logic allowing orphaned queued executions).

